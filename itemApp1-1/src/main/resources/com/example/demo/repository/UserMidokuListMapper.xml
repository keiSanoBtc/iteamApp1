<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.UserMidokuListMapper">
    <select id="userMidokuList" resultType="com.example.demo.entity.UserMidokuList">
	SELECT DISTINCT a.USER_NAME, c.BOOK_TITLE
	FROM (USER_TABLE a INNER JOIN BOOK_PLAN_TABLE b ON b.USER_ID = a.USER_ID)
	INNER JOIN BOOK_TABLE c ON b.BOOK_ID = c.BOOK_ID
	WHERE b.READ_FLAG = 0 AND b.DELETE_FLAG = 0
	ORDER BY a.USER_NAME;
    </select>
</mapper>